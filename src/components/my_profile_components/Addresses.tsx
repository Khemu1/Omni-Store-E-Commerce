import { Address } from "../index";

const Addresses = () => {
  return (
    <section className="flex justify-center font-lato">
      <div className="flex flex-col">
        <h2 className="font-semibold text-xl">Your Addresses</h2>
        <div className="flex"></div>
      </div>
    </section>
  );
};

export default Addresses;
